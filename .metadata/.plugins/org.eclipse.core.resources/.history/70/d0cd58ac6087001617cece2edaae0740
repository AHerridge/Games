package jrAlex.core;

import java.awt.Point;

public enum Direction
{
	NORTH(0, 1), EAST(1, 0), SOUTH(0, -1), WEST(-1, 0);

	public Point change;

	private Direction(int colShift, int rowShift)
	{
		change = new Point(colShift, rowShift);
	}
}
