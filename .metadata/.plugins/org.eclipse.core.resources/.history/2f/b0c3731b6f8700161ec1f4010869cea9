package jrAlex.core;

import com.google.gson.Gson;
import com.google.gson.JsonObject;

public class Main
{
	public static void main(String[] args)
	{
		Gson g = new Gson();
		World world = new World(64);
		world.addObject(new Entity(0, 0, 1, 1, world));
		JsonObject jobj = g.toJson(world);
	}
}
